import { createTheme } from "@mui/material";

const ThemeOptions = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#e0c01a',
    },
    secondary: {
      main: '#000000',
    },
  },
});

export default ThemeOptions


